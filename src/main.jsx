import React from 'react'
import ReactDOM from 'react-dom/client'
import BetterNotePad from './BetterNotePad.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BetterNotePad />
  </React.StrictMode>,
)
